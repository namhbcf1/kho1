{
  "name": "khoaugment-pos",
  "version": "1.0.0",
  "description": "KhoAugment POS - Production Ready Vietnamese Point of Sale System",
  "scripts": {
    "deploy:backend": "cd backend && wrangler deploy",
    "deploy:frontend": "cd frontend && npm run build && wrangler pages deploy dist --project-name=kho1",
    "deploy": "npm run deploy:backend && npm run deploy:frontend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build": "npm run build:backend && npm run build:frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "KhoAugment",
  "license": "UNLICENSED",
  "private": true
}
